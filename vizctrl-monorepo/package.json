{
  "name": "vizctrl-monorepo",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/react",
    "packages/maps-openlayers",
    "examples/demo-app"
  ],
  "scripts": {
    "build": "turbo run build --filter=*...",
    "test": "turbo run test --filter=*...",
    "dev:storybook": "turbo run storybook --filter=*...",
    "dev:app": "turbo run dev --filter=examples/demo-app"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "turbo": "^1.10.12"
  }
}
