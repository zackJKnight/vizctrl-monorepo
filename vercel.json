{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "name": "vizctrl-demo",
  "version": 2,
  "buildCommand": "yarn build",
  "outputDirectory": "examples/demo-app/dist",
  "installCommand": "yarn install --frozen-lockfile",
  "framework": "vite",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
